<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relay Race Registration</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-5">

    <h2 class="text-center mb-4">Relay Race Registration</h2>

    <form action="/register-relay" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
            <label><input type="radio" name="relayType" value="4x100M" required> 4x100M</label>
            <label class="ms-3"><input type="radio" name="relayType" value="4x400M" required> 4x400M</label>
        </div>
    
        <div id="students">
            <% for (let i = 0; i < 4; i++) { %>
                <div class="card p-3 mb-3">
                    <h5>Student <%= i+1 %></h5>
                    <div class="mb-2">
                        <label>Name:</label>
                        <input type="text" name="students[<%= i %>][name]" class="form-control" required>
                    </div>
                    <div class="mb-2">
                        <label>URN:</label>
                        <input type="text" name="students[<%= i %>][urn]" class="form-control" required>
                    </div>
                    <div class="mb-2">
                        <label>Upload Image:</label>
                        <input type="file" name="student<%= i+1 %>Image" class="form-control" accept="image/*" required>
                    </div>
                </div>
            <% } %>
        </div>
    
        <button type="submit" class="btn btn-primary">Register Relay Team</button>
    </form>

</body>
</html>
